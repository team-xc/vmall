// 保存数据
export const saveUsername = (state, username) => {
  state.username = username;
};

export const saveUsers = (state, users) => {
  state.users = users;
};

export const saveCarts = (state, carts) => {
  state.carts = carts;
};

export const saveOrders = (state, orders) => {
  state.orders = orders;
};