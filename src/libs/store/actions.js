export const saveUsername = ({ commit }, payload) => {
  commit("saveUsername", payload);
};

export const saveUsers = ({ commit }, payload) => {
  commit("saveUsers", payload);
};

export const saveCarts = ({ commit }, payload) => {
  commit("saveCarts", payload);
};

export const saveOrders = ({ commit }, payload) => {
  commit("saveOrders", payload);
};